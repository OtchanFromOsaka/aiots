## Branch Management

### Common parts (e.g. aio-date package)

1. create a new branch from `main` (e.g. `main-feat` )

```bash
git checkout main
git pull
git checkout -b main-feat
```

2. commit and push changes

3. squash merge into `main`

4. add tag and remove `main-feat` branch
